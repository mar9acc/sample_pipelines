jobs:
  - name: dv-man
    type: manifest
    steps:
      - IN: dv-image
        pull: false

  - name: multi-manifest
    type:  deploy
    steps:
      - IN: dv-man

  - name: release-one
    type: release
    steps:
      - IN: dv-man
      - TASK: managed
        bump: major

  - name: infra-provision
    type: runSh
    steps:
      - IN: dv-man
      - IN: testSshKey
      - TASK:
        - script: echo $TESTSSHKEY_NAME
        - script: echo $TESTSSHKEY_INTEGRATION_PUBLICKEY
        - script: echo $TESTSSHKEY_INTEGRATION_PRIVATEKEY
        - script: echo $TESTSSHKEY_PUBLIC_KEY_PATH
        - script: echo $TESTSSHKEY_PRIVATE_KEY_PATH
        
