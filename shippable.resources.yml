resources:
  - name: docker-image
    type: image
    integration: deepika-docker
    pointer:
      sourceName: "scriptnull/debian"
      #isPull: true
    seed:
      versionName: "driftking"

# # TEST resources

# # add TEST env - Triton cluster
  - name: joyent-dev-sw1
    type: cluster
    integration: deepika-triton
    pointer:
      region: "us-west-1"

# # Docker Image Options
  - name: docker-options
    type: dockerOptions
    version:
      memory: 128
      user: "driftking"
      workingDir: "/makemeworking"
      publishAllPorts: true
      cmd:
        - "node"
        - "job.js"
